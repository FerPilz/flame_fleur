import SwiftUI

enum AppShadow {
    static let cardColor = AppColors.shadow.opacity(0.08)
    static let cardRadius: CGFloat = 12
    static let cardYOffset: CGFloat = 5

    static let buttonColor = AppColors.shadow.opacity(0.10)
    static let buttonRadius: CGFloat = 10
    static let buttonYOffset: CGFloat = 4
}
